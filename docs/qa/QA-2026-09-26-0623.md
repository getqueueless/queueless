# Queueless QA — /doctor smoke test — 2026-09-26 06:23 IST (00:53 UTC)

Requested by "web motion" on behalf of the orchestrator: smoke-test the new doctor desk (`/doctor`) and `/admin/doctors`.

## Build outage (found, then fixed)

First attempt (~06:13–06:19 IST): `/doctor` and `/admin/doctors` returned real HTTP 404 for both staff and admin, despite the source being correct locally after a fresh pull. Isolated it to this specific commit batch — older routes (`/admin`) loaded fine. Flagged to "web motion" and "hackathon start" as a probable stuck/failed deploy.

Root cause (per web motion): a bare `kbd` selector in `doctor.module.css` failed the Turbopack production build, so the whole web deploy had been failing silently since ~06:12 and prod kept serving the previous build. Fixed in `79cd01a`, live by 06:22 IST (build ID changed).

## Re-test after the fix

**`/doctor` (admin) — PASS.** Loads correctly ("Doctor desk | Queueless"), picker lists all 8 doctors with room numbers. Selecting any doctor (tried Dr. Neha Sharma) shows a clean empty state: *"No desk for this doctor yet — Dr. Neha Sharma has no counter bound to them. An admin can bind one under Counters."* Confirmed via direct DB read that `counters.doctor_id` is null for every counter right now, so this applies to all 8 doctors — correct, graceful behavior, not a bug.

**N/S/D/R/K desk test — blocked, not a bug.** Can't exercise Next/Start/Done/Recall/Skip on `/doctor` until at least one counter is bound to a doctor (Hackathon database's pending work, per web motion's earlier note). Nothing to test here yet.

**`/admin/doctors` — PASS.** Works correctly on a fresh navigation: full doctor list, schedules, fees, plus a new "Login" action per doctor row (`DoctorLoginPanel`) and a new "Doctor desk" nav link. Zero console errors on either page.

**Note (not a bug):** navigating to `/admin/doctors` via an already-open `/doctor` tab's client-side router bounced back to `/doctor` once — reproduced only with that stale client state, not on a fresh tab. Not filing it; flagging in case it recurs elsewhere.

## Status

No P0. Waiting on the doctor-counter binding to run the live N/S/D/R/K + `/t` + `/display` test. Both findings messaged to "web motion" and "hackathon start".

Next: 06:50 IST demo-reset, 06:55 IST 10-minute final smoke across the full app per orchestrator's list.
