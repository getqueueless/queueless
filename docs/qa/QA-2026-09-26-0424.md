# Queueless QA — 0047 re-test — 2026-09-26 04:24 IST (22:54 UTC)

Target: production, https://lpu.lol / https://sb.lpu.lol. Tester: QA session, branch `qa`, report-only.

## 0047 (tokens/appointments/notifications/audit_log RLS) — CONFIRMED FIXED

Landed as `supabase/migrations/0047_tokens_appointments_notifications_audit_rls.sql`. Re-tested live against prod with a fresh patient JWT (`QA_PATIENT3`, an account with no relationship to any existing appointment/token):

| Table | Query | Before 0047 | After 0047 |
|---|---|---|---|
| `appointments` | own JWT, unfiltered | saw another patient's real booking | `content-range: */0`, `[]` |
| `tokens` | own JWT, unfiltered | saw arbitrary rows | `content-range: */0`, `[]` |
| `audit_log` | own JWT, unfiltered | 200, 5,148 rows (whole org) | 200, `[]` (RLS filters silently rather than erroring — patient has no `org_id`, `private.is_staff_of` correctly returns false) |
| `notifications` | own JWT, unfiltered | not previously re-checked | `[]` |

**One intentional carve-out remains, not a bug:** `anon` (no login at all) can still `SELECT` every row of `tokens` — `curl .../rest/v1/tokens?select=id,patient_id apikey:<anon>` still returns real rows for arbitrary patients. This is `tokens_read_anon_temporary`, a policy 0047 added on purpose, scoped identically to anon's pre-existing unrestricted grant (not a regression), because `/t/[id]` and `/pay/[tokenId]` still read the table directly. `0048_get_token_status_rpc.sql` gives `/t/[id]` a scoped RPC to replace that read; `/pay/[tokenId]` hasn't moved yet per the migration's own comment. Flagging for the next pass once both callers are off direct reads, so this policy can be dropped — not reporting it as a new finding since the team already knows and has a plan on record in the migration itself.

Sent this confirmation to the orchestrator (`hackathon start`) along with the live-lane-test result already on file in `QA-2026-09-26-0412.md`. No open P0.

Continuing the Phase 2 loop; final full regression scheduled for 06:30 IST per the orchestrator's ask (`docs/qa/QA-final.md`).
