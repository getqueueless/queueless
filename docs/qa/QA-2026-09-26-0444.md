# WaitWise QA — redirect re-test + cash-desk claim — 2026-09-26 04:44 IST (23:14 UTC)

## Open redirect — both payloads fixed

`01bac2a` closes the `next` open redirect for every control-character/backslash form. Re-tested both:
- `/login?next=/%5Cevil.com` — sign-in lands on `lpu.lol/my`.
- `/login?next=/%09/evil.com` (tab character) — sign-in lands on `lpu.lol/my`.

`location.hostname` confirmed `lpu.lol` for both. Closed.

## Cash-desk slip claim on `/my` — PASS

Context: earlier testing found kiosk-issued slips (`/kiosk`, web) can't be claimed via `/my`'s "Claim a ticket" because the kiosk form never captures a phone number, and claiming matches `profiles.phone = walkin_patients.phone`. The intended claimable path is the cash-desk walk-in flow (`staff_register_walkin` RPC, `supabase/migrations/0041_cash_desk.sql`), which does capture a phone — but it currently only has a UI on mobile (`apps/mobile/src/app/(app)/cash-desk.tsx`); `apps/web/src/lib/cash.ts` wraps the same RPC but nothing in `apps/web` imports it yet.

Tested the underlying path directly since no web UI exists: called `rpc/staff_register_walkin` via PostgREST with a real counter-staff JWT, phone set to match an existing patient account's stored `+91...` number exactly. Then signed in as that patient and used "Claim a ticket" on `/my` with the returned code.

**Result: works correctly.** Landed on the token's own `/t/<id>`, `tokens.patient_id` confirmed set to the claiming patient via direct read, dashboard shows it under "Your token" and "Recent visits". Negative case also checked: claiming a web-kiosk code (no phone on file) correctly shows "We couldn't match that ticket to your phone number" with no crash.

Both sent to the orchestrator. No new P0.

Continuing toward the 06:05 IST final regression.
