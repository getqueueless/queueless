# WaitWise QA — fix re-verification — 2026-09-26 04:40 IST (23:10 UTC)

## Open redirect P0 — FIXED, confirmed

Landed as `01bac2a fix(auth): close the post-login open redirect through a backslash in next`. Re-tested `/login?next=/%5Cevil.com`: sign-in now correctly lands on `https://lpu.lol/my` (`location.hostname === "lpu.lol"`). Closed.

## Counter-state live push — fix attempt landed, still not fixed

`9259017 fix(staff): re-read desk states on every Counter refetch so Call next enables after opening` landed. Re-tested the exact scenario from the earlier report:

1. `counter1` on `/counter` (OPD-2, `Open`).
2. Admin closes OPD-2 via `/admin/counters` — confirmed saved on the admin's own list.
3. `counter1`'s tab, left untouched: still showed `Open` / Call Next enabled after 34+ seconds.
4. Issued an unrelated OPD token from a different staff session, to see if any refetch trigger the commit added would pick it up — no change.
5. Manual reload of `counter1`'s tab: immediately correct, `Closed` / Call Next disabled.

So ground truth is right, the fix didn't add a live push/subscription for counter-state changes reaching an already-open `/counter` tab with no interaction from that tab. Still P1, workaround is a manual reload. Messaged this back to the orchestrator so it doesn't get marked closed by mistake.

## Still open, no fix yet

- 91-prefixed phone number truncated by `/my/profile`'s `maxLength=10` (see `QA-2026-09-26-0436.md`).

Continuing toward the 06:05 IST final regression.
