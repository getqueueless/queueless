# cp .env.example .env, then fill in real values. .env is gitignored -- never commit it.
QL_API_TARGET=host.docker.internal:8000
POSTGRES_EXPORTER_DSN=postgresql://ql_metrics:change-me@localhost:5432/postgres?sslmode=disable
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_TO=admin@lpu.lol
GRAFANA_ADMIN_PASSWORD=change-me
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
ALERTMANAGER_PORT=9093
POSTGRES_EXPORTER_PORT=9187
